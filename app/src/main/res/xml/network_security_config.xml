<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Network Security Configuration for DAM (Device Audit & Monitoring)

        Security Policy:
        - Debug builds: Allow HTTP for emulator testing (10.0.2.2)
        - Release builds: HTTPS only, block all cleartext traffic
        - Production: Certificate pinning ready for api.production.com
    -->

    <!-- Allow cleartext for local development (physical phone or emulator) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.1.115</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.1.6</domain>
        <domain includeSubdomains="true">172.18.1.18</domain>
    </domain-config>

    <!-- Default configuration: HTTPS only (production) -->
    <domain-config cleartextTrafficPermitted="false">
        <!-- Main production API endpoint -->
        <domain includeSubdomains="true">api.production.com</domain>
    </domain-config>

    <!-- Debug-only overrides: Allow cleartext for emulator development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

</network-security-config>
